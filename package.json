{
  "name": "@sammacbeth/dat-api",
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run clean --parallel && lerna clean --yes",
    "build": "lerna run build --concurrency 1",
    "test": "lerna run test",
    "lint": "lerna run lint --parallel"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sammacbeth/sams-dat-api.git"
  },
  "bugs": {
    "url": "https://github.com/sammacbeth/sams-dat-api/issues"
  },
  "publishConfig": {
    "access": "public"
  }
}
