{
  "name": "@sammacbeth/dat-api",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run clean && lerna clean --yes",
    "build": "lerna run build",
    "test": "lerna run test"
  }
}
